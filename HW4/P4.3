if [ "$#" -ne 1 ]
then
	echo "Missing parameter"
fi

file1=$1

if [ ! -f "$file1" ]
then
	echo "File does not exist!"
fi

while IFS= read -r line
do
	word_count=$(echo $line | wc -w)
	if [ "$word_count" -eq 4 ]
	then
		echo "$line"
	fi
done < "$file1"
